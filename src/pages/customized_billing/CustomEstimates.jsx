import React from "react";

const CustomEstimates = () => {
  return (
    <div>
      <h2>Custom Estimates</h2>

      <div className="empty-box">
        No custom estimates created yet.
      </div>

      <button className="primary-btn">➕ Create New Estimate</button>
    </div>
  );
};

export default CustomEstimates;
